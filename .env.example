# Copy to .env on the server and fill in real values. Never commit .env.

# --- Server ---
PORT=5000
NODE_ENV=production

# --- Database (Postgres) ---
# Local Postgres on EC2:  postgres://niytri:PASSWORD@localhost:5432/niytri
# Or an RDS endpoint:     postgres://user:pass@xxxx.rds.amazonaws.com:5432/niytri
DATABASE_URL=

# --- Microsoft 365 email (Graph API, client-credentials) ---
# From your Entra ID app registration (see DEPLOYMENT.md step 4)
M365_TENANT_ID=
M365_CLIENT_ID=
M365_CLIENT_SECRET=
# Mailbox the emails are sent from
M365_SENDER=admin@niytri.com
# Comma-separated recipients for new-enquiry notifications
ENQUIRY_NOTIFY_TO=admin@niytri.com,amit@niytri.com

# --- Admin ---
# Random secret; required to read enquiries via GET /api/enquiries (X-Api-Key header)
ADMIN_API_KEY=
